package standard.search.binary;

/**
 * 局部最小
 * arr 数组 长度为N 无序，
 * 1、arr[0] < arr[1]  arr[0] 为局部最小
 * 2、arr[N-1] < arr[N-2] arr[N-1] 为局部最小
 * 3、arr[i -1] > arr[i] < arr[i+1] arr[i] 为局部最小
 *
 * 返回任意一个局部最小的
 */
public class PartitionMin {

    public static int oneMin(int[] arr){
        if(arr == null ){

        }

        return  0;
    }
}
